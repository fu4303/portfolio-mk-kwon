https://web.dev/prerender-with-react-snap/

hydrate() is used when we want to render our React Application on server side and hydrate the JavaScript bundle on the client side 
which make our application fast and also allow the search engines to crawl your pages for SEO purposes.

